*) Make sure simplejdbcmapper version is updated

*) Make sure <autoPublish>true</autoPublish>

*) Make sure the tests pass
mvn clean package

*) make sure th check sonarqube

*) To deploy
mvn clean deploy -DskipTests


